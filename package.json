{
  "name": "json-server",
  "version": "1.0.0",
  "description": "",
  "main": "mock.js",
  "scripts": {
    "script": "npm run build && node dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && babel src -D --out-dir dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^2.6.1",
    "body-parser": "^1.18.3",
    "csvtojson": "^2.0.8",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.26.0",
    "rimraf": "^2.6.2"
  }
}
